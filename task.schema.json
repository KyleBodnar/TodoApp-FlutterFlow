{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/task.schema.json",
  "title": "Task Document",
  "description": "Schema for a Firestore task document in the ToDo app.",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Short title of the task."
    },
    "details": {
      "type": "string",
      "description": "Detailed description of the task."
    },
    "completed": {
      "type": "boolean",
      "description": "Whether the task has been completed."
    },
    "created": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the task was created."
    },
    "user": {
      "type": "string",
      "description": "Reference to the user document (Firestore document reference path)."
    }
  },
  "required": ["title", "completed", "created", "user"],
  "additionalProperties": false
}
